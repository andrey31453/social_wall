services:
  nest:
    build:
      context: .
      dockerfile: dockerfile
      target: nest_dev
    command: npm run dev
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
